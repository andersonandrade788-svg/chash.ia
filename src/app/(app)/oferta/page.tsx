import { OfferGenerator } from '@/components/offer/offer-generator'

export const metadata = { title: 'Gerador de Oferta — Cash.IA' }

export default function OfertaPage() {
  return <OfferGenerator />
}
