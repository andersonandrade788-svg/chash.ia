import { CourseGenerator } from '@/components/course/course-generator'

export const metadata = { title: 'Gerador de Curso — Cash.IA' }

export default function CursoPage() {
  return <CourseGenerator />
}
