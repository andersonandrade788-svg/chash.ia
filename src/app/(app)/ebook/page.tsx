import { EbookGenerator } from '@/components/ebook/ebook-generator'

export const metadata = { title: 'Gerador de eBook — Cash.IA' }

export default function EbookPage() {
  return <EbookGenerator />
}
